class Solution {
  public:
    int countFreq(vector<int>& arr, int target) {
        int occ=0;
        for(int i=0;i<arr.size();i++){
            if(arr[i]==target){
                occ++;
            }
        }return occ;
    }
};
